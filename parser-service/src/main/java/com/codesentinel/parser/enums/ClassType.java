package com.codesentinel.parser.enums;

public enum ClassType {
    CONTROLLER,
    REST_CONTROLLER,
    SERVICE,
    REPOSITORY,
    ENTITY,
    CONFIGURATION,
    COMPONENT,
    INTERFACE,
    ENUM,
    OTHER
}